This is a Next.js app with Storybook support.

## Getting Started

Run the app:

```bash
yarn dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

Run Storybook:

```bash
yarn storybook
```

The generated starter page, demo stories, and stock assets have been removed so the project starts from a clean baseline.
